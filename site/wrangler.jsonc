{
  // Nitro generates .output/server/wrangler.json on every build and merges this
  // file into it. `main` and `assets` are always set by the build — don't add
  // them here or the build warns and ignores them.
  //
  // `name` is pinned for two reasons. Nitro otherwise auto-derives it from the
  // git repo and folder ("lidowe-ulm-organizing-site"), which would become the
  // *.workers.dev hostname. More importantly, Workers Builds FAILS the build
  // when this disagrees with the Worker's name in the Cloudflare dashboard.
  //
  // This must therefore match the dashboard Worker exactly. It is
  // "ulm-organizing" because that is the default name Cloudflare assigned when
  // the repository was imported. Renaming a Worker means deleting and
  // recreating it, so the config matches the Worker rather than the reverse.
  // Only the *.workers.dev address depends on this; the custom domains below
  // are what the site is actually served on.
  "name": "ulm-organizing",

  // Pinned so the Workers runtime behaviour doesn't silently shift between
  // builds — nitro defaults this to the date the build ran.
  "compatibility_date": "2026-08-05",
  "compatibility_flags": ["nodejs_compat"],

  // Pinned here for the same reason as everything else in this file: settings
  // that live only in the dashboard can be reset by a deploy.
  //
  // This is on because the site is not static. /api/public/inquiry already
  // logs "inquiry delivery failed" when Resend or the webhook rejects a
  // submission, and without log persistence nobody ever sees it — a contact
  // form that quietly stops working is the worst failure this site has.
  //
  // head_sampling_rate 1 logs every request. Sampling below 1 exists for
  // high-traffic Workers; at this volume partial logs would just hide the one
  // failure worth catching. Retention is 7 days.
  //
  // observability.traces is deliberately absent: it is distributed tracing for
  // multi-service apps, free only during its beta, and there is nothing here
  // for it to trace.
  "observability": {
    "enabled": true,
    "head_sampling_rate": 1,
    "logs": { "invocation_logs": true }
  },

  // Attaching the domains here means the first successful deploy also creates
  // the DNS records and issues the certificate — no dashboard step needed.
  // `custom_domain: true` routes ALL paths of the hostname to this Worker.
  //
  // If a deploy fails here, the usual cause is an existing CNAME record on the
  // hostname: Cloudflare refuses to create a Custom Domain over one. Delete the
  // old record in DNS → Records, then redeploy.
  "routes": [
    { "pattern": "upperlevelmusic.com", "custom_domain": true },
    { "pattern": "www.upperlevelmusic.com", "custom_domain": true }
  ]
}
